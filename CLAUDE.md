# CLAUDE.md

见 `AGENTS.md`（本项目 AI 协作的唯一根入口与最高行为规范摘要）。

启动时请先完整阅读 `AGENTS.md`，并严格按其"会话启动协议"执行：

1. 通读 `AGENTS.md`
2. 读取 `dev-docs/rules.md` 与 `dev-docs/status.md`
3. 复述当前阶段、铁律要点、当前授权、下一个待办任务
4. 停止，等待用户指派——此时不得创建或修改任何文件

若无法读取上述文件，必须明确告知用户，不得凭记忆或猜测行事。

## 文档间状态冲突的仲裁规则

若发现 `dev-docs/` 内多份文件对同一事实的描述不一致（例如 `backlog.md` 的复选框状态与 `status.md`/`history.md` 的叙述矛盾），以 `dev-docs/status.md` 为准，并向用户报告该冲突，不得自行判断哪份文件"应该"是对的。
